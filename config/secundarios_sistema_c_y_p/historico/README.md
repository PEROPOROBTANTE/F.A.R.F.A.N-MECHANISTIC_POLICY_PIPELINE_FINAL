Histórico y material obsoleto.

- Contiene artefactos descontinuados (`legacy_*`, reportes, capas antiguas).
- Todos con `_metadata.status: legacy` o `_deprecated: true`; `_live: false`.
- No usar en runtime; referencia solo para auditoría o trazabilidad.
