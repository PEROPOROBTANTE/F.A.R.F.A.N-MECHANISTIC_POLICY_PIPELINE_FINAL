# Secundarios - Histórico

Zona de archivos obsoletos/no vigentes.

Reglas:
- Solo mover aquí material explícitamente descartado.
- Debe llevar `_metadata` con `status: legacy` y `_live: false`.
- No usar en runtime.
